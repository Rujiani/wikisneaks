# Copy to .env.test and point at a dedicated test database — not the same DB as .env:
#   cp .env.test.example .env.test
#
# Used by:
#   npm run test
#   npm run test:watch
#   npm run test:migrate

DATABASE_URL="postgresql://USER:PASSWORD@localhost:5433/test_db"
NODE_ENV="test"
